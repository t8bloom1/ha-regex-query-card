/**
 * Visual configuration editor for the Regex Query Card
 * This file is intentionally minimal as the card uses schema-based configuration
 */

// Editor functionality is handled by Home Assistant's built-in schema editor
// The card's configuration schema is defined in the main card file
export {};